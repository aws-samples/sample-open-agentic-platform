# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# # Code generated from CUE definitions. DO NOT EDIT.
apiVersion: core.oam.dev/v1beta1
kind: TraitDefinition
metadata:
  annotations:
    definition.oam.dev/alias: ""
    definition.oam.dev/description: Give a workload a secretless identity to AgentGateway via a projected ServiceAccount token
  name: gateway-identity
spec:
  appliesToWorkloads:
    - deployments.apps
    - rollouts.argoproj.io
  conflictsWith: []
  podDisruptive: true
  schematic:
    cue:
      template: |
        parameter: {
        	// +usage=Audience stamped into the projected token; must match the gateway's expected audience
        	audience: *"agentgateway" | string
        	// +usage=Container to mount the token into (defaults to the component name)
        	containerName: *context.name | string
        }

        _mountDir: "/var/run/secrets/agentgateway"
        _tokenMount: {
        	name:      "agentgateway-token"
        	mountPath: _mountDir
        	readOnly:  true
        }

        patch: spec: template: spec: {
        	// +patchKey=name
        	volumes: [{
        		name: "agentgateway-token"
        		projected: sources: [{
        			serviceAccountToken: {
        				audience:          parameter.audience
        				expirationSeconds: 3600
        				path:              "token"
        			}
        		}]
        	}]
        	// +patchKey=name
        	containers: [{
        		name: parameter.containerName
        		// +patchKey=name
        		env: [{
        			name:  "WORKLOAD_TOKEN_PATH"
        			value: "\(_mountDir)/token"
        		}]
        		// +patchKey=name
        		volumeMounts: [_tokenMount]
        	}]
        }
  workloadRefPath: ""

