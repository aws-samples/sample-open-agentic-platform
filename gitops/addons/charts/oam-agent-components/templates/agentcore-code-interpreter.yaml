# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# # Code generated from CUE definitions. DO NOT EDIT.
apiVersion: core.oam.dev/v1beta1
kind: ComponentDefinition
metadata:
  annotations:
    definition.oam.dev/alias: ""
    definition.oam.dev/description: AgentCore CodeInterpreter provisioned via Crossplane managed resource with IAM policy
  name: agentcore-code-interpreter
spec:
  schematic:
    cue:
      template: |
        import "strings"
        let _autoName = strings.Replace(context.namespace+"_"+context.name, "-", "_", -1)
        output: {
        	apiVersion: "bedrockagentcore.aws.upbound.io/v1beta1"
        	kind:       "CodeInterpreter"
        	metadata: name: context.name
        	spec: {
        		forProvider: {
        			name:        parameter.codeInterpreterName
        			region:      parameter.region
        			description: parameter.description
        			networkConfiguration: {
        				networkMode: parameter.networkMode
        			}
        		}
        		providerConfigRef: name: "default"
        	}
        }
        outputs: "\(context.name)-iam-policy": {
        	apiVersion: "iam.aws.upbound.io/v1beta1"
        	kind:       "Policy"
        	metadata: name: "\(context.appName)-\(context.name)-iam-policy"
        	spec: {
        		forProvider: {
        			name: "\(context.appName)-\(context.name)-iam-policy"
        			policy: """
        				{
        				  "Version": "2012-10-17",
        				  "Statement": [
        				    {
        				      "Effect": "Allow",
        				      "Action": ["bedrock-agentcore:*"],
        				      "Resource": "*"
        				    }
        				  ]
        				}
        				"""
        		}
        		providerConfigRef: name: "default"
        	}
        }
        parameter: {
        	// +usage=CodeInterpreter name in AWS. Defaults to <namespace>_<componentName>
        	codeInterpreterName: *_autoName | string
        	// +usage=AWS region
        	region: *"us-east-1" | string
        	// +usage=Description of the code interpreter
        	description: *"AgentCore Code Interpreter" | string
        	// +usage=Network mode: PUBLIC or VPC
        	networkMode: *"PUBLIC" | "VPC"
        }
  status:
    customStatus: |-
      message: *("codeInterpreterId: " + context.output.status.atProvider.codeInterpreterId) | "provisioning"
      codeInterpreterId: *context.output.status.atProvider.codeInterpreterId | ""
    healthPolicy: 'isHealth: *( context.output.status.atProvider.codeInterpreterId != "" ) | false'
  workload:
    type: autodetects.core.oam.dev
