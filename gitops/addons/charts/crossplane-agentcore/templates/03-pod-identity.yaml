{{/*
  IAM Role and Pod Identity Association for the Crossplane BedrockAgentCore provider.
  This gives the provider permissions to create/manage AgentCore resources.

  DISABLED BY DEFAULT (podIdentity.createViaChart: false). On spoke clusters
  the PodIdentityAssociation cannot be reconciled locally because spokes do
  not run an EKS-API-capable Crossplane provider (they never provision
  clusters). Instead, the agentcore provider's Pod Identity is provisioned by
  the hub's Crossplane via the pod-identities overlay
  (gitops/overlays/environments/<env>/pod-identities/values.yaml), which
  targets the spoke cluster from the hub.
*/}}
{{- if .Values.podIdentity.createViaChart }}
apiVersion: iam.aws.upbound.io/v1beta1
kind: Role
metadata:
  name: {{ .Values.global.eksClusterName }}-agentcore-provider-role
  labels:
    platform.gitops.io/role: agentcore-provider
    platform.gitops.io/cluster: {{ .Values.global.eksClusterName }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  forProvider:
    assumeRolePolicy: |
      {
        "Version": "2012-10-17",
        "Statement": [{
          "Effect": "Allow",
          "Principal": {"Service": "pods.eks.amazonaws.com"},
          "Action": ["sts:AssumeRole", "sts:TagSession"]
        }]
      }
    inlinePolicy:
      - name: AgentCoreFullAccess
        policy: |
          {
            "Version": "2012-10-17",
            "Statement": [{
              "Effect": "Allow",
              "Action": [
                "bedrock-agentcore:*",
                "bedrock:InvokeAgentCoreTool"
              ],
              "Resource": "*"
            }]
          }
    tags:
      platform.gitops.io/role: agentcore-provider
      platform.gitops.io/cluster: {{ .Values.global.eksClusterName }}
  providerConfigRef:
    name: default
---
apiVersion: eks.aws.upbound.io/v1beta1
kind: PodIdentityAssociation
metadata:
  name: {{ .Values.global.eksClusterName }}-agentcore-provider
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  forProvider:
    clusterName: {{ .Values.global.eksClusterName }}
    namespace: {{ .Values.podIdentity.namespace }}
    serviceAccount: {{ .Values.podIdentity.serviceAccountName }}
    roleArnSelector:
      matchLabels:
        platform.gitops.io/role: agentcore-provider
        platform.gitops.io/cluster: {{ .Values.global.eksClusterName }}
    region: {{ .Values.global.awsRegion }}
  providerConfigRef:
    name: default
{{- end }}
