{{/*
  The bedrockagentcore provider uses the existing 'default' ProviderConfig
  which already has Pod Identity credentials via the family provider.
  The Pod Identity association (03-pod-identity.yaml) grants the provider's
  ServiceAccount the necessary Bedrock AgentCore permissions.
*/}}
