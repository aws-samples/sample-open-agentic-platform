apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: my-assistant
  namespace: default
spec:
  components:
    - name: assistant
      type: agent
      properties:
        name: oap-assistant-a
        namespace: default
        description: "General purpose AI assistant with A2A protocol support"
        
        # agent image
        # image: 498530348755.dkr.ecr.us-west-2.amazonaws.com/strands-agent:latest
        
        # Multiple replicas with consistent hashing
        replicas: 3
        
        # System prompt
        systemMessage: |
          You are a helpful AI assistant with access to various tools and capabilities.
          You can help with general questions, data analysis, and task automation.
          Always be clear, concise, and helpful in your responses. When asked about time use the tools.
        
        # Model configuration
        modelConfig:
          modelId: claude-sonnet
        
        # Service account for AWS permissions (if needed)
        serviceAccount: default
        
        # Blue-green deployment settings
        autoPromotionEnabled: true
        autoPromotionSeconds: 10
        scaleDownDelaySeconds: 30
        
        # AgentGateway registration
        registerWithGateway: true
        # Memory — AgentCore (native Strands session manager, not mem0)
        # memory:
        #   provider: agentcore
        #   config:
        #     memoryId: my-agentcore-mem
        #     region: us-west-2
        mcpServers:
          - name: mcp-time
        # Resource limits
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
